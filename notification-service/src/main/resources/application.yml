server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: notification-service

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
